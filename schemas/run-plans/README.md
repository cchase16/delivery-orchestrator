# Run-Plan Schema

Canonical phased plan for executing one approved work package. It defines stable tasks, dependencies, roles, requested model profiles, allowed repositories and paths, validation gates, approval gates, statuses, and required evidence. Domain factories may extend it with domain-specific fields.
