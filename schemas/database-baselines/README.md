# Database-Baseline Schema

References a database and filestore lineage without embedding either artifact. It records source environment, snapshot identifier, capture time, Odoo and module versions, product commit, sanitization state, classification, storage reference, and permitted uses.
