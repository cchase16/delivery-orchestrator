# Command Schemas

`command.schema.json` defines version 1 of uniquely identified human and orchestrator commands. Commands must be attributable, idempotent, scoped to one target, and validated against the current workflow state.

Review `examples/start-run.example.json` for a non-secret illustrative command.
