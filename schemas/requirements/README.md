# Requirement Schema

Canonical immutable requirement revision. Required concepts include stable ID, revision, source provenance, human-readable document, status, priority, dependencies, acceptance-criterion identifiers, affected capabilities, and content digest.
