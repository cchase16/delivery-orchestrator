# Model-Profile Schema

Named capability profiles that resolve to an allowed model, reasoning effort, fallback policy, and execution adapter. Each execution records the requested profile and actual resolved settings.
